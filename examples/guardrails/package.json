{
  "name": "guardrail-middleware-example",
  "version": "0.1.0",
  "description": "Simplified guardrail middleware showing smart routing and intent classification",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "node --test dist/**/*.test.js",
    "lint": "eslint src",
    "clean": "rm -rf dist"
  },
  "keywords": [
    "guardrails",
    "middleware",
    "routing",
    "intent-classification",
    "token-optimization"
  ],
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  }
}
